const search=document.querySelector('#shop-search');
if(search){const category=document.querySelector('#shop-category');const groups=[...document.querySelectorAll('.shop-group')];const filter=()=>{let total=0;const query=search.value.trim().normalize('NFC').toLocaleLowerCase('th');for(const group of groups){let count=0;for(const row of group.querySelectorAll('[data-shop]')){const visible=(category.value==='all'||category.value===group.dataset.category)&&row.dataset.shop.normalize('NFC').toLocaleLowerCase('th').includes(query);row.hidden=!visible;if(visible)count++;}group.hidden=count===0;total+=count;}document.querySelector('#results').textContent=`แสดง ${total} ร้าน`;document.querySelector('#empty').hidden=total!==0;};search.addEventListener('input',filter);category.addEventListener('change',filter);}
const map=document.querySelector('.map-panel');if(map&&matchMedia('(min-width: 640px)').matches)map.open=true;
