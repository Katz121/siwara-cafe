# งานตรวจเว็บ takuapa101.com · รอบตรวจความถูกต้อง

โฟลเดอร์งาน `D:\Siwaracafeweb\output\web\takuapa101-v1`
เว็บจริง `https://takuapa101.com` · ไทย 45 หน้า · อังกฤษ 44 หน้า

## กฎการทำงาน (ทุกคนต้องอ่าน)

1. **ห้ามแก้ไฟล์ใด ๆ** งานนี้คือตรวจและรายงานเท่านั้น · เจ้าของเว็บจะเป็นคนตัดสินใจแก้
2. **ทุกข้อที่รายงานต้องมีหลักฐาน** ระบุไฟล์ บรรทัด หรือ URL ที่ตรวจเจอ · ห้ามเขียนว่า "อาจจะ" ลอย ๆ
3. **แยกความรุนแรง** เป็น 3 ระดับ
   - `ผิด` = ข้อมูลผิด ลิงก์เสีย โค้ดพัง ผู้ใช้เจอปัญหาจริง
   - `ควรแก้` = ไม่ผิดแต่ทำให้ดีกว่านี้ได้ชัดเจน
   - `ข้อสังเกต` = เรื่องรสนิยมหรือทางเลือก
4. **ห้ามรายงานสิ่งที่ยังไม่ได้ตรวจจริง** ถ้าตรวจไม่ได้ให้เขียนว่าตรวจไม่ได้เพราะอะไร
5. ถ้าไม่เจอปัญหาในหัวข้อไหน ให้เขียนว่า "ตรวจแล้วไม่พบปัญหา" พร้อมบอกว่าตรวจอะไรไปบ้าง
   **อย่าแต่งปัญหาขึ้นมาเพื่อให้มีอะไรรายงาน**
6. เขียนรายงานเป็นภาษาไทย ใช้ `·` แทน em-dash · เขียนไฟล์เป็น UTF-8 เท่านั้น

## บริบทที่ต้องรู้ก่อนตรวจ

เว็บนี้ตั้งใจ **แยกข้อเท็จจริงที่ยืนยันแล้วออกจากที่ยังไม่ยืนยัน** อย่างชัดเจน
คำว่า "ยังไม่ยืนยัน" "สันนิษฐาน" "แหล่งขัดกัน" ที่เห็นบนหน้าเว็บ **ไม่ใช่ข้อบกพร่อง**
แต่เป็นสิ่งที่ตั้งใจใส่ · อย่ารายงานว่าเป็นปัญหา

ทำนองเดียวกัน ชื่อหน่วยงานราชการไทยในหน้าอังกฤษที่เขียนเป็น
`สำนักงานนโยบายและแผน... (Office of Natural Resources...)` **ตั้งใจคงภาษาไทยไว้**
เพื่อให้คนตามไปหาเอกสารต้นทางได้ ไม่ใช่คำแปลตกหล่น

ไฟล์อ้างอิงสำคัญ
- `data/places-enriched.json` ข้อมูลสถานที่ 27 แห่ง ทุก claim มี status และ source_url
- `data/photo-registry.json` ทะเบียนรูป กำหนดว่ารูปไหนขึ้นหน้าไหนได้
- `data/stories/*.json` บทความ 5 บท
- `data/guide-picks.json` คู่มือคัดสรร 13 รายการ
- `data/shops-extra.json` ร้านจากการค้นหา 49 ร้าน
