# แก้ลิงก์แหล่งอ้างอิงที่ตายแล้ว

- เพิ่ม `data/dead-sources.json` บันทึก 4 URL/prefix ที่ตรวจเมื่อ 8 ก.ย. 2569
- เพิ่ม `dead_sources.py` เป็นฟังก์ชันกลาง `source_link(url, label)` และใช้กับการ render แหล่งอ้างอิงใน `stories.py`, `place_pages.py`, `design.py` และ `build_site.py`
- เพิ่ม guard ใน `build_en.py` ให้หน้าอังกฤษแสดงชื่อแหล่งเดิมเป็นข้อความ พร้อมหมายเหตุอังกฤษ
- ลิงก์ที่ตรงกับรายการตายถูกเปลี่ยนเป็นข้อความทั้งหมด โดยยังคงชื่อแหล่งและ claim ไว้
- ผลตรวจหลัง build: ไทยพบหมายเหตุ 51 จุด, อังกฤษ 7 จุด; ไม่พบลิงก์ตายเหลือใน `site/` และ build ได้ 44 + 44 หน้า
